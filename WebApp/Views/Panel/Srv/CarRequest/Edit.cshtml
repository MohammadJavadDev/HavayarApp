@using Entities.App.Gnr
@using Entities.App.Hcm
@using Entities.App.Srv
@using Entities.App.Srv.Enums
@using Entities.Base
@model CarRequest

<div class="card shadow-sm border-0">
	<div class="form-action-buttons border-bottom p-3">
		<form-action-buttons entity-type="typeof(CarRequest)"></form-action-buttons>
	</div>
	<div class="card-body">
		<input type="hidden" data-bind="id" value="@Model?.Id" />
		<input type="hidden" data-bind="htsId" value="@Model?.HtsId" />

		<div class="row mb-3">
			<div class="col-12"><h5 class="fw-bold mb-0">درخواست خودرو</h5></div>
		</div>
		<div class="row g-3 mb-4">
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold required">محل خدمت / شعبه</label>
				<select class="form-control" data-bind="branchId" data-required="true"
						asp-for="BranchId" asp-items="Html.GetEnumSelectList(typeof(CarRequestBranchEnum))">
					<option value="">انتخاب کنید</option>
				</select>
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold required">نوع</label>
				<select class="form-control" data-bind="typeId" data-required="true"
						asp-for="TypeId" asp-items="Html.GetEnumSelectList(typeof(CarRequestTypeEnum))">
					<option value="">انتخاب کنید</option>
				</select>
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold">مسافر</label>
				@(Html.EntitySelector<Personel>(
					"CarRequestPassengerSelector",
					c => c.IsActive == IsActiveEnum.Active,
					c => new { Id = c.Id, Name = c.Name, Family = c.Family, FamilyDisplay = c.FamilyDisplay, Code = c.Code },
					c => new { c.Name, c.Family, c.FamilyDisplay, c.Code },
					"{Name} {FamilyDisplay}",
					"passengerId",
					null,
					Model?.PassengerId > 0 ? Model.PassengerId.ToString() : null,
					false
				))
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold required">تاریخ نیاز</label>
				<input class="form-control" data-bind="needDateInText" data-required="true"
					   data-persionDatePicker="true"
					   persion-datetimepicker='{"format":"YYYY/MM/DD","autoClose": true,"initialValue": false}'
					   value="@(Model?.NeedDate != default ? Model.NeedDate.ToString("yyyy-MM-dd") : "")" />
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold required">ساعت نیاز</label>
				<input type="text" class="form-control ltr" data-bind="needTime" data-time="true" data-required="true"
					   placeholder="__:__" value="@Model?.NeedTime" />
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold required">مرکز هزینه</label>
				@(Html.EntitySelector<CostCenter>(
					"CarRequestCostCenterSelector",
					c => c.IsActive == IsActiveEnum.Active,
					c => new { Id = c.Id, Title = c.Title, Number = c.Number },
					c => new { c.Title, c.Number },
					"{Title}",
					"costCenterId",
					null,
					Model?.CostCenterId > 0 ? Model.CostCenterId.ToString() : null,
					true
				))
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold required">مدت انتظار</label>
				<input type="text" class="form-control ltr" data-bind="waitingTime" data-required="true"
					   value="@(Model?.WaitingTime?.ToString(System.Globalization.CultureInfo.InvariantCulture) ?? "")" />
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold required">مقصد</label>
				<input class="form-control" data-bind="destination" data-required="true" value="@Model?.Destination" />
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-4 form-group-inline">
				<label class="form-label fw-bold">تعداد نفرات</label>
				<input type="text" class="form-control ltr" data-bind="countPeople"
					   value="@(Model?.CountPeople?.ToString() ?? "")" />
			</div>
			<div class="col-md-12 form-group-inline">
				<label class="form-label fw-bold">توضیحات</label>
				<textarea class="form-control" data-bind="description" rows="2">@Model?.Description</textarea>
			</div>
		</div>
	</div>
</div>

<script>
	function savefn($btnAction) {
		if (validateError(page.$pageEl)) return;
		const model = page.$pageEl.dataBind();
		const $btn = $($btnAction.element).block();
		$$.post('save', model, function (r) {
			$btn.block(false);
			if (!r.isSuccess) {
				toastr.error(r.message, 'خطا');
				return;
			}
			toastr.success('ذخیره سازی با موفقیت انجام شد');
			page.$pageEl.dataBind(r.data);
			$btnAction.baseaction();
		});
	}
	self.FormActionButtons.save.onclick(function () { savefn(this); });
	self.FormActionButtons.saveandnew.onclick(function () { savefn(this); });
	self.FormActionButtons.saveandclose.onclick(function () { savefn(this); });
</script>
