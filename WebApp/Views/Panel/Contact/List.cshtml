@using Entities.App
 
<datatableprofile entity-type="typeof(Contact)"></datatableprofile>
<script>


	var d= "";

     debugger
          if (data.table) {
         data.table.onRowRender(function(rowData, $row) {
              
             // بررسی فیلد وضعیت
                  if (rowData.c11 == "فعال" ) {
                 $row.css('background-color', '#d4edda'); // سبز روشن
             }
         });
     }

	
</script>