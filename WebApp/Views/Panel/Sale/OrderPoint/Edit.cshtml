@using Entities.App.Inv
@using Entities.App.Sale
@using Entities.Base
@model OrderPoint

<div class="card shadow-sm border-0">
	<div class="form-action-buttons border-bottom p-3">
		<form-action-buttons entity-type="typeof(OrderPoint)">
			<form-action-button color-class="btn-active-icon-dark btn-color-info" icon-class="fa fa-comment"
				action-name="openComments" title="یادداشت‌ها"></form-action-button>
		</form-action-buttons>
	</div>
	<div class="card-body row">
		<span class="section-box-title">جزئیات</span>
		<input class="form-control" data-bind="id" value="@Model?.Id" type="hidden" />
		<input class="form-control" data-bind="htsId" value="@Model?.HtsId" type="hidden" />
		<input class="form-control" data-bind="htsPartCardexId" value="@Model?.HtsPartCardexId" type="hidden" />
			<div class="col-md-6 form-group-inline">
				<label class="form-label fw-bold">کالای جایگزین</label>
				@(Html.EntitySelector<Part>(
					"OrderPointAlternativePartSelector",
					c => c.IsActive == IsActiveEnum.Active,
					c => new { Id = c.Id, Code = c.Code, Name = c.Name },
					c => new { c.Code, c.Name },
					"{Code} {Name}",
					"alternativePartId",
					"alternativePartName",
					Model?.AlternativePartId?.ToString()
				))
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-2 form-group-inline">
				<label class="form-label fw-bold">عدم تایید</label>
				<input class="form-check-input mt-2" type="checkbox" data-bind="isIgnore" asp-for="IsIgnore" />
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-2 form-group-inline">
				<label class="form-label fw-bold">خارجی</label>
				<input class="form-check-input mt-2" type="checkbox" data-bind="isExternal" asp-for="IsExternal" />
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
			<div class="col-md-12 form-group-inline">
				<label class="form-label fw-bold">توضیحات</label>
				<textarea class="form-control" data-bind="comment" asp-for="Comment" rows="3"></textarea>
				<div data-invalidmessagespan class="my-1 mx-1"><span class="text-danger"></span></div>
			</div>
	</div>
</div>
<script>
	function savefn($btnAction) {
		if (validateError(page.$pageEl)) return;
		const model = page.$pageEl.dataBind();
		const $btn = $($btnAction.element).block();
		$$.post('save', model, function (r) {
			$btn.block(false);
			if (!r.isSuccess) { toastr.error(r.message, 'خطا'); return; }
			toastr.success('ذخیره سازی با موفقیت انجام شد');
			page.$pageEl.dataBind(r.data);
			$btnAction.baseaction();
		});
	}
	self.FormActionButtons.save.onclick(function () { savefn(this); });
	self.FormActionButtons.saveandnew.onclick(function () { savefn(this); });
	self.FormActionButtons.saveandclose.onclick(function () { savefn(this); });
	if (self.FormActionButtons.openComments) {
		self.FormActionButtons.openComments.onclick(function () {
			const id = page.$pageEl.dataBind().id;
			if (!id) { toastr.error('ابتدا ذخیره کنید'); return; }
			appController.addPage('/Panel/Sale/OrderPointComment/ListByParentId?orderPointId=' + id, true, 'یادداشت نقطه سفارش');
		});
	}
</script>
